# RATE_API_TOKEN below is the tripladev/rate-api simulator's own published test
# token (from its Docker Hub page), not a real secret. It's committed on purpose
# so a reviewer can `cp .env.example .env` from a clean clone and run this
# without hunting for credentials first.
RATE_API_URL=http://rate-api:8080
RATE_API_TOKEN=04aa6f42aa03f220c2ae9a276cd68c62
